name: K6 Load Test CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch: # Permite ejecución manual

jobs:
  k6_load_test:
    name: Run K6 Load Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install dependencies
        run: npm install

      - name: Start Express Server
        run: npm start &
      
      - name: Wait for server
        run: npx wait-on http://localhost:3000/health --timeout 30000

      - name: Setup k6
        uses: grafana/setup-k6-action@v1

      - name: Run k6 load test
        uses: grafana/run-k6-action@v1
        with:
          path: test/load/load-test.js

      # Paso opcional para enviar a Grafana Cloud (requiere secretos)
      # - name: Run k6 load test and push to Grafana Cloud
      #   uses: grafana/k6-action@v0.3.1
      #   with:
      #     filename: test/load/load-test.js
      #     flags: --out prometheusremote=url=${{ secrets.GRAFANA_REMOTE_WRITE_URL }}
      #   env:
      #     K6_PROMETHEUS_REMOTE_USER: ${{ secrets.GRAFANA_REMOTE_WRITE_USER }}
      #     K6_PROMETHEUS_REMOTE_PASSWORD: ${{ secrets.GRAFANA_REMOTE_WRITE_TOKEN }}
